<?php 
    $this->headTitle()->prepend('Egresos');
    echo $this->headTitle();
?>
<div id="egresos_container">
    <h1>Egresos<a style="float: right; text-decoration: none"href="/egresos/nuevo">Nuevo</a></h1>
    <?php if(count($successMessages)) :?>
        <div class="units-row">
            <?php foreach ($successMessages as $message) :?>
                <div class="unit-100">    
                    <div class="tools-alert tools-alert-green">
                        <?php echo $message?>
                    </div> 
                </div>
            <?php endforeach;?>
        </div>
    <?php endif;?>
    <div class="units-row">
        <div class="unit-100">
            <fieldset>
                <legend>Filtros</legend>
                <div class="units-row">
                    <div class="unit-30">
                        <label>Clínica</label>  
                        <select class="width-100" name="clinica_filter">
                            <?php foreach ($clinicas as $key => $value) :?>
                            <option value="<?php echo $key?>"><?php echo $value?></option>
                            <?php endforeach;?>
                        </select>
                    </div>
                    <div class="unit-30">
                        <label>Concepto</label>
                        <select class="width-100" name="concepto_filter">
                            <?php foreach ($conceptos as $key => $value) :?>
                            <option value="<?php echo $key?>"><?php echo $value?></option>
                            <?php endforeach;?>
                        </select>
                    </div>
                    <div class="unit-40" id="filter_container">
                        <label style="display: block">Filtrar por periodo de tiempo</label>
                        <input placeholder="Desde" type="email" name="egreso_from" class="width-6" />
                        <input placeholder="Hasta" type="email" name="egreso_to" class="width-6" />
                        <button id="filterbydate" class="btn btn-blue" style="height: 30px;"><i class="fa fa-filter"></i> </button>
                    </div>
                </div>
            </fieldset>
        </div>
    </div>
    <table>
        <thead>
            <tr>                
                <th>Fecha</th>
                <th>Clínica</th>
                <th>Empleado</th>
                <th>Concepto</th>
                <th>Gasto</th>
                <th>IVA</th>
                <th>Nota</th>
                <th>Comprobante</th>
                <th>Opciones</th>   
            </tr>
        </thead>
        <tbody>

        </tbody>
    </table>
</div>
<script src="/js/egresos/egresos.js"></script>
<script>
  $('#egresos_container').egresos();
</script>